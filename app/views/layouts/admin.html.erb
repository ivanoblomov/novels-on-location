<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset=utf-8 />
    <meta name=mobile-web-app-capable content=yes />
    <title><%= default_title %></title>
    <link rel=canonical href="<%= request.url %>"/>
    <link rel="shortcut icon" href="/favicon.ico"/>
    <%= stylesheet_link_tag '1140', 'admin' %>
    <%= csrf_meta_tag %>
  </head>
  <body>
    <%= yield %>
  </body>
</html>
