<%- if @location.valid? -%>
  addPin(<%= inject_writable_flag(@location).to_json.html_safe -%>);
<%- else -%>
  alert("Can't find the book '<%= escape_javascript params[:location][:book_keywords] -%>'!");
<%- end -%>
