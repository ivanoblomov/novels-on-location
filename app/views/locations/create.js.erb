<% if @location.valid? %>
nOL.addPin(<%= inject_writable_flag(@location).to_json.html_safe %>);
nOL.showPin('<%= @location.id %>');
nOL.promptForNotes('<%= @location.id %>', null);
<% else %>
alert("Can't find the book '<%= j params[:location][:book_keywords] %>'!");
<% end %>