<%- if @location.valid? -%>
  addPin(<%= to_json(inject_writable_flag @location).html_safe -%>);
<%- else -%>
  alert("Can't find the book '<%= escape_javascript params[:location][:book_keywords] -%>'!");
<%- end -%>
