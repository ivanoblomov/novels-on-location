<%- if @location.valid? -%>
  addPin('<%= @location.id -%>', [<%= @location.lat_lng * ',' -%>], '<%= render('location').strip.html_safe -%>', true);
<%- else -%>
  alert("Can't find the book '<%= escape_javascript params[:location][:book_keywords] -%>'!");
<%- end -%>
