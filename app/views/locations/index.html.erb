<div id="map-canvas"></div>

<div id="places-search-box">
  <%= text_field_tag 'place_criteria', 'Find a place', :class => 'search', :id => 'place-input' -%>
</div>

<div id="books-search-box">
  <%= text_field_tag 'book_criteria', 'Find a mapped book', :class => 'search', :id => 'book-input' -%>
</div>

<script type="text/javascript">
//<![CDATA[
  var locations = [
    <%-
      @locations.each do |@location|
    -%>
      {'id': '<%= @location.id -%>', 'title': '<%= @location.title -%>', 'latLng': <%= to_google_lat_lng(@location.lat_lng) -%>, 'content': '<%= render("location").strip -%>'},
    <%- end -%>
  ];
//]]>
</script>
